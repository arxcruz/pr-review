# AGENTS

Instructions and repository configuration for AI agents.

## Agent skills

### Issue tracker

Local markdown files under `.scratch/`. Never stage or commit anything under `.scratch/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical five-role triage vocabulary. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout (`CONTEXT.md` and `docs/adr/` at the root). See `docs/agents/domain.md`.
